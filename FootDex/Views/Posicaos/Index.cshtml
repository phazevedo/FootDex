@model IEnumerable<FootDex.Models.Posicao>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Setor.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Setor.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PosicaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PosicaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PosicaoId })
        </td>
    </tr>
}

</table>
